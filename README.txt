"FinalGUI.py" is the final application that uses client feedback and improvements, CLI is the OLD VERSION.

Before running "FinalGUI.py", please ensure that the file IS NOT in a OneDrive folder or the leaderboard may not function properly.